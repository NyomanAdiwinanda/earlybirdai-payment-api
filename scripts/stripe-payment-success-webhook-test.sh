# Trigger stripe webhook using the stripe-cli
# Make sure to setup the stripe-cli first from official documentation: https://docs.stripe.com/stripe-cli?install-method=homebrew
# When stripe-cli is setup, you can run the script

stripe trigger payment_intent.succeeded